set -x
set -e

which cmake


if [ ! -d CLONES ]; then

    git clone https://github.com/albertocasagrande/CLONES.git

    (cd CLONES; git checkout 30facd2 && rm -rf .git )

fi

${R_HOME}/bin/R --vanilla --silent -e 'Rcpp::compileAttributes()'
